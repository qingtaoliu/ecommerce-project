// 导出所有API服务
import { authAPI, productsAPI, cartAPI, ordersAPI, userAPI, categoryAPI, addressAPI } from '@repo/api';

export {
  authAPI,
  productsAPI,
  cartAPI,
  ordersAPI,
  userAPI,
  categoryAPI,
  addressAPI
};
